# Railway deploy konfigürasyonu
# https://railway.app - CLI: railway up
#
# Not: data/ klasörü repo ile birlikte deploy edilir.

[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npm install && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
